name: Different Compare
on: [push]
jobs:
  different_compare:
    name: Self Compare
    runs-on: Ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run a script
      run: diff file.txt different.txt
